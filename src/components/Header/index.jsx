import { AppBar, Container, Toolbar } from '@mui/material'

const Header = () => {
  return (
    <AppBar>
      <Container>
        <Toolbar>
          {/*Logo*/}
          {/*Navbar*/}
          {/*Button*/}
        </Toolbar>
      </Container>
    </AppBar>
  )
}

export default Header
